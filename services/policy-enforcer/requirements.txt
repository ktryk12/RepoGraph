# Policy Enforcer Service Requirements - Stage 1

# Core dependencies
asyncio
pydantic>=2.0.0
aiofiles>=23.0.0
asyncpg>=0.28.0

# Event bus
aiokafka>=0.8.0

# Configuration and utilities
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Logging
structlog>=23.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Stage 2+ dependencies (commented for future)
# opa-python>=0.1.0  # OPA Python bindings
# wasm>=0.1.0        # WASM runtime for compiled policies