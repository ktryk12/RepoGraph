# RepoGraph
RepoGraph is a zero-setup, locally-run code intelligence engine that indexes any codebase into a persistent graph — capturing every function, class, import, and call chain — and exposes it through a REST API and MCP server so AI agents like Claude Code get full architectural awareness before making changes.
